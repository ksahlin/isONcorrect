"""isONcorrect: de novo error correction of long-read transcriptome reads.

This file must exist for `[tool.setuptools.packages.find]` to see this directory
as a package -- `namespaces = false` in pyproject.toml means a directory without
an `__init__.py` is skipped. It was missing from the repository (though present in
the published 0.1.3.5 sdist, where it had been picked up as an untracked file), so
`python -m build` produced a wheel containing the two console-script entry points
and no code at all. Do not delete it.

The Python implementation is deprecated; see the README.
"""
